<?xml version='1.0' encoding='utf-8' ?>
<odoo>
    <!--    <record id="ticket_action_from_project" model="ir.actions.act_window">-->
    <!--        <field name="name">Tickets</field>-->
    <!--        <field name="res_model">helpdesk.ticket</field>-->
    <!--        <field name="view_mode">kanban,tree,form</field>-->
    <!--        <field name="search_view_id" ref="helpdesk_mgmt.helpdesk_ticket_view_search" />-->
    <!--    </record>-->
    <!-- TREE TICKET HELP DESK -->
    <record id="ticket_view_tree" model="ir.ui.view">
        <field name="model">helpdesk.ticket</field>
        <field name="inherit_id" ref="helpdesk_mgmt.ticket_view_tree" />
        <field name="arch" type="xml">
            <field name="project_id" position="after">
                <field name="task_id" optional="hide" />
                <field name="sprint_ids" optional="hide" widget="many2many_tags" />
                <field name="us_id" optional="hide" />
            </field>
        </field>
    </record>
    <!-- SEARCH TICKET HELP DESK -->
    <record id="helpdesk_ticket_view_search" model="ir.ui.view">
        <field name="model">helpdesk.ticket</field>
        <field name="inherit_id" ref="helpdesk_mgmt.helpdesk_ticket_view_search" />
        <field name="arch" type="xml">
            <field name="task_id" position="after">
                <field name="us_id" />
            </field>
            <filter name="group_stage" position="after">
                <filter string="Project" name="group_project" domain="[]" context="{'group_by':'project_id'}" />
                <filter string="Task" name="group_task" domain="[]" context="{'group_by':'task_id'}" />
                <filter string="User Stories" name="group_us" domain="[]" context="{'group_by':'us_id'}" />
            </filter>
        </field>
    </record>
    <!-- View Form Ticket -->
    <record id="ticket_view_form" model="ir.ui.view">
        <field name="model">helpdesk.ticket</field>
        <field name="inherit_id" ref="helpdesk_mgmt_project.ticket_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='project_id']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='task_id']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='partner_email']/.." position="after">
                <group string="Project">
                    <field name="project_id" />
                    <field name="project_use_scrum" invisible="1" />
                    <field
                        name="task_id"
                        domain="[('project_id', '=', project_id)]"
                        attrs="{'invisible':['|',('project_id', '=', False),('us_id', '!=', False)]}"
                        context="{'default_project_id': project_id}"
                    />
                    <field
                        name="task_id"
                        domain="[('project_id', '=', project_id),('us_id', '=', us_id)]"
                        attrs="{'invisible':['|',('project_id', '=', False),('us_id', '=', False)]}"
                        context="{'default_project_id': project_id}"
                    />
                    <field
                        name="sprint_ids"
                        domain="[('project_id', '=', project_id)]"
                        attrs="{'invisible':['|',('project_id', '=', False),('project_use_scrum', '=', False)]}"
                        context="{'default_project_id': project_id}"
                        widget="many2many_tags"
                    />
                    <field
                        name="us_id"
                        domain="[('project_id', '=', project_id)]"
                        attrs="{'invisible':['|',('project_id', '=', False),('project_use_scrum', '=', False)]}"
                        context="{'default_project_id': project_id}"
                    />
                </group>
            </xpath>
        </field>
    </record>
</odoo>
